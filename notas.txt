revisar Schema JSON-LD al final del proyecto

Actúa como un Senior Frontend Performance Engineer especializado en optimización extrema para Core Web Vitals, Lighthouse y PageSpeed.

Proyecto:
Sitio web corporativo de seguridad electrónica llamado SmartHome.
Objetivo:
llevar el sitio a:

* Performance mobile: 90+
* Performance desktop: 98+
* SEO: conservar 100
* Accessibility: conservar 95+
* Best Practices: conservar 95+

Regla principal:
NO cambiar diseño visual, branding, layout, copy ni funcionalidades existentes.
Optimizar solamente arquitectura frontend, assets, carga, render y estructura técnica.

Realiza una auditoría completa del proyecto y aplica TODAS las mejoras posibles.

## 1) Optimización extrema de imágenes

Auditar todas las imágenes del proyecto y aplicar:

* convertir PNG/JPG/WebP a AVIF donde sea compatible
* mantener fallback WebP/JPG
* generar múltiples tamaños responsive:

  * 320w
  * 640w
  * 960w
  * 1280w
  * 1920w

Reemplazar todas las etiquetas img por:

* srcset
* sizes
* width
* height
* decoding="async"
* loading="lazy" (excepto hero above the fold)
* fetchpriority="high" solo para hero principal/logo si corresponde

Comprimir agresivamente sin pérdida visual perceptible.

Eliminar imágenes cargadas a resolución superior a la mostrada.

Auditar backgrounds CSS pesados:

* convertir a AVIF/WebP
* usar image-set()
* responsive backgrounds

## 2) Reemplazar GIFs

Buscar todos los GIFs animados.

Si existe:
animated-icon.gif u otros gifs:

reemplazar por:

opción preferida:
video WebM VP9 alpha transparent loop autoplay muted playsinline

estructura:

<video autoplay muted loop playsinline preload="none">
 <source src="archivo.webm" type="video/webm">
 <img src="fallback.webp" alt="">
</video>

Reducir peso mínimo 90%.

Si animación puede recrearse:
usar CSS animation o SVG animation.

## 3) CSS crítico

Extraer Critical CSS above-the-fold:

incluir inline:

* header
* navbar
* hero
* CTA principal
* primer viewport

Todo CSS restante:
cargar no bloqueante.

usar:

<link rel="preload" as="style">
<link rel="stylesheet" media="print" onload="this.media='all'">

Eliminar render blocking CSS.

Eliminar CSS duplicado.

Eliminar CSS no usado.

Minificar todo.

## 4) Eliminar dependencia de Bootstrap

Auditar uso de Bootstrap.

Migrar:

* grid → CSS Grid/Flex
* spacing → utility classes propias
* botones → componentes CSS propios
* navbar → propia
* modals → propia
* accordions → propia

Eliminar:

* bootstrap.min.css
* bootstrap.bundle.js
* bootstrap-icons

Reemplazar iconos por:

* SVG inline
  o sprite SVG

Reducir CSS/JS al mínimo.

## 5) Optimización JS

Auditar todo JS.

Aplicar:

* tree shaking
* code splitting
* lazy import
* defer
* async
* eliminar librerías innecesarias
* minificación máxima
* eliminar código muerto
* eliminar listeners duplicados
* throttling/debouncing donde aplique

Inicializar componentes solo cuando entren en viewport usando IntersectionObserver.

No ejecutar JS innecesario en mobile.

## 6) Reemplazar Swiper

Buscar sliders/carousels.

Eliminar Swiper.

Reemplazar por:

prioridad:

* CSS scroll snap
  o
* Embla Carousel minimal

Mantener UX igual.

Reducir peso.

## 7) Fonts

Auditar fuentes.

Aplicar:

* self-hosted
* woff2
* subset latin
* preload
* font-display: swap

Eliminar variantes no usadas.

Máximo 1 familia / 2 weights si es posible.

## 8) Third-party scripts

Auditar:

* GTM
* GA4
* pixels
* embeds
* chats
* maps
* widgets

Reglas:

eliminar lo innecesario.

Mover third-party a lazy load post-interaction.

Analytics:
si GTM solo se usa para Analytics:
reemplazar GTM por GA4 directo.

Consent mode correcto.

No bloquear render.

## 9) Caché

Configurar:

assets versionados hash filename:

app.a84df.css
hero.f91ab.avif

Headers:

Cache-Control: public,max-age=31536000,immutable

HTML:
no-cache corto.

gzip + brotli.

## 10) Preload / preconnect

Agregar estratégicamente:

preconnect:

* fonts
* analytics
* CDN necesarios

preload:

* hero
* logo
* font principal
* CSS crítico

No abusar.

## 11) DOM

Reducir DOM depth.

Eliminar wrappers innecesarios.

Reducir nesting.

Eliminar elementos ocultos no usados.

Mantener HTML semántico.

## 12) CLS

Garantizar CLS ~0:

reservar espacio:

* imágenes
* embeds
* sliders
* banners
* widgets

usar aspect-ratio.

## 13) Lazy everything

Lazy load:

* imágenes below fold
* videos
* iframes
* mapas
* sliders
* secciones pesadas

Usar IntersectionObserver.

## 14) Accesibilidad

Mantener o mejorar:

* labels
* aria
* contraste
* navegación teclado
* focus visible
* landmarks

## 15) SEO técnico

Mantener 100/100.

Verificar:

* canonical
* sitemap
* robots
* schema.org
* OG tags
* Twitter cards
* headings hierarchy
* alt text
* internal linking
* crawlability

No romper indexación.

## 16) Seguridad

Agregar:

* CSP correcta
* X-Frame-Options
* X-Content-Type-Options
* Referrer-Policy
* Permissions-Policy

## 17) Service Worker

Evaluar cache inteligente:

stale-while-revalidate

solo si mejora UX.

No romper SEO.

## 18) Lighthouse target

Optimizar hasta lograr:

LCP < 2 s
INP < 200 ms
CLS < 0.02
TTFB mínimo
Total Blocking Time mínimo

## 19) Entregable

Modificar archivos reales del proyecto.

Luego entregar:

1. lista exacta de cambios
2. archivos tocados
3. peso antes/después
4. requests antes/después
5. Core Web Vitals estimados
6. mejoras adicionales detectadas
7. problemas de arquitectura encontrados
8. sugerencias premium opcionales

Trabajar con criterio de producción enterprise.
No hacer cambios cosméticos.
Solo mejoras reales.
